# Книжный магазин

> [🇬🇧 English](README.md) | 🇷🇺 Русский

Веб-приложение интернет-магазина книг на Django. Просматривайте каталог, регистрируйтесь и оформляйте заказы.

## Возможности

- Каталог книг с обложками, описаниями и статусом наличия
- Страница детального просмотра книги
- Регистрация и авторизация пользователей
- Оформление заказов с проверкой остатка на складе
- История личных заказов
- Постраничная навигация каталога
- Адаптивный интерфейс на Bootstrap 5
- Панель администратора Django для управления контентом

## Стек технологий

| Уровень | Технология |
|---|---|
| Backend | Python 3, Django 5 |
| База данных | SQLite (по умолчанию) |
| Frontend | Bootstrap 5.3 |
| Обработка изображений | Pillow |

## Требования

- Python 3.10+
- pip

## Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd exam_app

# Создать и активировать виртуальное окружение
python -m venv .venv
source .venv/bin/activate      # Linux / macOS
.venv\Scripts\activate         # Windows

# Установить зависимости
pip install -r requirements.txt

# Применить миграции
python manage.py migrate

# (Опционально) Создать аккаунт администратора
python manage.py createsuperuser

# Запустить сервер разработки
python manage.py runserver
```

Приложение будет доступно по адресу `http://127.0.0.1:8000/`.

## Переменные окружения

Для продакшн-развёртывания задайте следующие переменные окружения:

| Переменная | Описание | По умолчанию |
|---|---|---|
| `SECRET_KEY` | Секретный ключ Django | небезопасный ключ для разработки |
| `DEBUG` | Режим отладки (`True`/`False`) | `True` |
| `ALLOWED_HOSTS` | Список разрешённых хостов через запятую | _(пусто)_ |

## Запуск тестов

```bash
python manage.py test
```

## Структура проекта

```
exam_app/
├── exam_app/          # Настройки проекта и корневой URL-конфиг
├── main/              # Приложение: модели, представления, формы, шаблоны
│   ├── migrations/    # Миграции базы данных
│   ├── templates/     # HTML-шаблоны
│   ├── models.py      # Модели Book и Order
│   ├── views.py       # Функции-представления
│   ├── forms.py       # Классы форм
│   ├── admin.py       # Конфигурация администратора
│   └── tests.py       # Набор тестов
├── media/             # Загружаемые пользователями файлы (обложки книг)
├── manage.py
└── requirements.txt
```

## Лицензия

[MIT](LICENSE)
